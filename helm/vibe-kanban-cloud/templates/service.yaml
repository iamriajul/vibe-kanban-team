apiVersion: v1
kind: Service
metadata:
  name: {{ include "vibe-kanban-cloud.fullname" . }}
  labels:
    {{- include "vibe-kanban-cloud.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "vibe-kanban-cloud.selectorLabels" . | nindent 4 }}
